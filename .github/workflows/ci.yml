name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  changes:
    name: Detect paths
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
      ai: ${{ steps.filter.outputs.ai }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            web:
              - 'apps/web/**'
              - 'scripts/ci/**'
              - '.github/workflows/**'
              - 'AGENTS.md'
              - 'docs/plans/2026-08-05-standing-decisions.md'
            ai:
              - 'services/ai/**'
              - 'scripts/ci/**'
              - '.github/workflows/**'
              - 'AGENTS.md'
              - 'docs/plans/2026-08-05-standing-decisions.md'

  web:
    name: Web
    needs: changes
    if: needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/web
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - name: Install
        run: npm ci
      - name: Lint (incl. anti-slop)
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Test
        run: npm test
      - name: File size caps
        working-directory: .
        run: node scripts/ci/check-file-size.mjs --root apps/web --max 400 --ext .ts,.tsx
      - name: CI helper tests (file size)
        working-directory: .
        run: node --test scripts/ci/check-file-size.test.mjs

  ai:
    name: AI
    needs: changes
    if: needs.changes.outputs.ai == 'true'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: services/ai
    steps:
      - uses: actions/checkout@v4
      # Node is required for the shared file-size helper (scripts/ci/check-file-size.mjs).
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Sync
        run: uv sync --all-groups
      - name: Ruff check
        run: uv run ruff check .
      - name: Ruff format
        run: uv run ruff format --check .
      - name: Pytest
        run: uv run pytest
      - name: File size caps
        working-directory: .
        run: node scripts/ci/check-file-size.mjs --root services/ai --max 500 --ext .py
      - name: Function size caps
        working-directory: .
        run: python3 scripts/ci/check_python_function_size.py --root services/ai --max 60
      - name: CI helper tests (function size)
        run: uv run pytest ../../scripts/ci/test_check_python_function_size.py

  # Single required check for branch protection (path-skipped jobs report skipped).
  ci:
    name: CI
    needs: [changes, web, ai]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Aggregate
        run: |
          set -euo pipefail
          changes="${{ needs.changes.result }}"
          web="${{ needs.web.result }}"
          ai="${{ needs.ai.result }}"
          echo "changes=$changes web=$web ai=$ai"
          if [[ "$changes" != "success" ]]; then
            echo "Path detection failed: $changes"
            exit 1
          fi
          if [[ "$web" == "failure" || "$web" == "cancelled" ]]; then
            echo "Web job failed ($web)"
            exit 1
          fi
          if [[ "$ai" == "failure" || "$ai" == "cancelled" ]]; then
            echo "AI job failed ($ai)"
            exit 1
          fi
          if [[ "$web" != "success" && "$web" != "skipped" ]]; then
            echo "Unexpected web result: $web"
            exit 1
          fi
          if [[ "$ai" != "success" && "$ai" != "skipped" ]]; then
            echo "Unexpected ai result: $ai"
            exit 1
          fi
          if [[ "$web" == "skipped" && "$ai" == "skipped" ]]; then
            echo "No package paths changed — nothing to verify."
          fi
          echo "CI gate passed"
