"use client";
import { useState } from "react";
import type { Classe } from "@/lib/verdia-domain";
export function OverrideForm({id, current}:{id:string;current:Classe|null}){const [classe,setClasse]=useState<Classe>(current??"baixa");const [motivo,setMotivo]=useState("");const [loading,setLoading]=useState(false);async function save(){if(motivo.trim().length<5){alert("Informe o motivo da correção.");return}setLoading(true);const r=await fetch(`/api/capturas/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({classeFinal:classe,motivo})});setLoading(false);if(!r.ok){alert((await r.json()).error??"Falha");return}location.reload()}return <div className="override"><select className="select" value={classe} onChange={e=>setClasse(e.target.value as Classe)}><option value="baixa">Baixa</option><option value="média">Média</option><option value="alta">Alta</option></select><textarea className="input" rows={4} placeholder="Motivo da correção manual..." value={motivo} onChange={e=>setMotivo(e.target.value)}/><button className="btn btn-primary" onClick={save} disabled={loading}>{loading?"Salvando…":"Aplicar correção"}</button></div>}
