# Datasets (deferred)

Public-dataset obtain stubs under `raw/` (TAS500, forefield, DNIT) belonged to the
abandoned hand-CV / cobertura labeling track. They are **not** on the VLM prototype
path. Keep the stubs for later reference; do not treat them as required for
`verdia_ai.vlm` / `python -m verdia_ai.classify`.
